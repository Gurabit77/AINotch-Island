# AINotch Island v1.0.0 使用说明

把 MacBook 顶部刘海区做成"AI 进程的灵动岛"——监控你跑的所有 AI 编程助手、显示音量电池/网络/AirDrop 等系统状态、还能在通知岛里直接审批 AI 工具的权限请求。

适用：**macOS 14.6 及以上**，Apple Silicon / Intel 都支持。

---

## 1. 安装

### 1.1 双击挂载 DMG

双击 `AINotch Island v1.0.0.dmg`，挂载后把左边 **AINotch Island.app** 拖到右边 **Applications** 文件夹图标上。

### 1.2 首次启动（重要）

因为这是开源版、未走 Apple Developer ID 公证，macOS 会拦：

> "无法验证开发者，因此打不开 AINotch Island"

**正确做法**：

1. 打开「应用程序」，**右键**点 `AINotch Island` → 选「打开」
2. 弹出确认框，再点「打开」
3. 之后双击启动就不会再拦

或者在「系统设置 → 隐私与安全性」最下方点「仍要打开」。

> 这是开源软件分发的标准流程，跟"信任开发者证书"不是一回事——这个 app 完全本地运行，**不联网回传任何东西**（除自动更新检查）。

### 1.3 首次启动会要的权限

| 权限 | 用来做什么 | 不给会怎样 |
|---|---|---|
| **蓝牙** | 显示 AirPods 连接 / 电量提示 | 蓝牙类提示不出现，其他正常 |
| **辅助功能 / 屏幕录制** | 监听全局快捷键 / 监测屏幕录制状态 | 全局快捷键失效 |
| **通知** | 告诉你 AI 跑完 / 需要审批 | 你得自己盯岛 |
| **本地网络** | 显示 WiFi 名 | WiFi 信息不全 |

每项弹出系统对话框时点「允许」即可。**不强制全开**，按你的需求挑。

---

## 2. 一上手能用的功能

启动后等 2-3 秒，刘海区出现像素螃蟹小伙伴（Buddy）——说明 app 起来了。

### 2.1 灵动岛会自动出现的场景

| 场景 | 触发条件 |
|---|---|
| 🔊 音量条 | 按音量键 |
| ☀️ 亮度条 | 按亮度键 |
| ⌨️ 键盘背光 | 按键盘背光键 |
| 🔋 充电 / 低电量 / 充满 | 接/拔充电器 |
| 🎵 NowPlaying | 播放音乐时 |
| 📥 AirDrop | 别人 AirDrop 给你时 |
| 📡 WiFi / VPN / 热点 / 断网 | 网络状态变化 |
| 🎧 蓝牙耳机连接 | AirPods 等连上时 |
| 🎬 屏幕录制 | 系统级录屏开始 |
| 📸 截图 | 用 Cmd+Shift+3/4 时 |
| 🌙 专注模式 | 开/关勿扰 |
| 💾 外接硬盘 | 插 U 盘 / 移动硬盘 |
| ⏰ 计时器 | 系统时钟 app 计时 |
| ⬇️ 下载 | 浏览器/系统下载进行中 |
| 🔒 锁屏 | 即将锁屏前几秒 |

### 2.2 菜单栏图标

启动后菜单栏右上角会有一只像素螃蟹图标（带数字 = 当前活跃的 AI 会话数）。点它弹出 popover：

- 上方：每个 AI 会话一张卡片（在做什么、用了什么工具）
- 下方四个按钮：
  - ⚙️ **Settings** — 打开设置窗口
  - ⬇️ **Check for Updates** — 检查更新
  - 🔄 **Restart** — 重启 app
  - ⏏️ **Quit** — 退出

### 2.3 全局快捷键

| 按键 | 作用 |
|---|---|
| `⌘⇧A` | 展开 / 收起 AgentHalo 面板 |
| `⌘⇧Y` | 一键批准最近一个待审批的 AI 请求 |
| `⌘⇧J` | 跳转到当前活跃 AI 会话所在的终端 |
| `⌘⇧K` | 杀掉主活跃 AI 进程（紧急停手用） |
| `⌘⇧D` | 关掉所有待处理的审批弹窗 |

---

## 3. 核心功能：AI 进程监控（AgentHalo）

这是这个 app 的**灵魂功能**。它会自动监听你电脑上跑的所有 AI 编程助手。

### 3.1 支持的 AI 工具（开箱即用 22+ 种）

Claude Code、Codex、Cursor、Kiro、Windsurf、Gemini CLI、Hermes、OpenClaw、Amp、Aider、DeepSeek、Copilot、MiMo Code 等等。

### 3.2 两种监测方式

- **Hooked（实时事件）**：自动给 Claude Code、Codex、Cursor、Amp 安装 hook，它们做什么你立刻看到（哪个工具调用、什么命令、需不需要审批）
- **Scanned（进程扫描）**：其他工具用进程扫描发现

首次启动时会自动尝试为支持的工具安装 hook，不需要你做什么。

### 3.3 岛上看到什么

跑 AI 时，灵动岛会显示：

- **当前在做什么**：例如 `Bash: ls`、`Read: file.swift`、`Edit: foo.py`
- **右侧数字**：当前活跃会话数
- **打勾标记**：刚完成一个工具调用
- **三色状态**：🟢 工作中 / 🟠 待审批 / 🔴 出错

### 3.4 审批请求

AI 想跑一条 bash 命令、改文件，或者用 AskUserQuestion 问你问题时——

- **紧凑模式**（默认）：岛上出现一个橙色小条
  - 普通工具调用 → 三个按钮：拒绝 / 允许 / 始终允许
  - AskUserQuestion 这种带选项的 → 显示「选项」蓝色标，点开岛展开看全部选项
- **展开模式**：点岛或按 `⌘⇧A`，能看完整的请求详情、diff、选项列表、风险等级
- **快捷键审批**：`⌘⇧Y` 一键允许

### 3.5 跳转到对应终端

点会话卡片或按 `⌘⇧J`，自动定位到那个 AI 跑的终端窗口。支持：iTerm2、Ghostty、Warp、Terminal.app、Kitty、WezTerm、VS Code 内置终端、Cursor 内置终端。

---

## 4. 设置（菜单栏 → Settings）

主要分类：

| 分类 | 能调什么 |
|---|---|
| **General** | 启动行为、外观主题（亮/暗/跟随系统）、缩放比例 |
| **Notch** | 岛的形状、动画、特定 LiveActivity 优先级 |
| **Battery** | 充电/低电量/充满弹岛的开关与触发阈值 |
| **Bluetooth** | 蓝牙连接弹岛与电量显示 |
| **Network** | WiFi 名显示、VPN 检测、热点 |
| **Focus** | 专注模式弹岛 |
| **HUD** | 音量/亮度/键盘背光弹岛 |
| **Lock Screen** | 锁屏前的过渡动画 |
| **Now Playing** | 音乐源过滤、歌词显示 |
| **Downloads** | 下载监听、来源过滤 |
| **Drag and Drop** | AirDrop / 文件托盘 / 文件转换 |
| **Timer** | 系统计时器弹岛 |
| **Agent Halo** | AI 监控核心配置（外观、行为、静音规则、声音、子项） |
| **Showcase** | ⭐ 一键演示所有岛场景（录功能演示用） |
| **About** | 版本、License、致谢 |

### 4.1 Agent Halo 子项详解

- **Appearance** — 卡片密度、Buddy 显示开关
- **Behavior** — 自动通知、孤儿进程清理、token 显示
- **Silence Rules** — 屏蔽某些会话不在岛上显示（例如长跑后台的 daemon）
- **Sounds** — 工具完成 / 审批弹起的提示音主题

### 4.2 Showcase（演示模式）

Settings → Showcase → **Run Full Showcase**

会按顺序播放岛能呈现的所有场景（充电、网络、AirDrop、AI 工具等等 33 种），后面再单独展示 30 种 buddy 表情动画。适合：

- 录功能演示视频
- 检查每种场景的样式
- 给朋友 / 同事演示这个 app

中途想停就再点一次按钮（变成 Stop）。

---

## 5. 卸载

如果不想用了：

```bash
# 1. 退出 app
osascript -e 'tell application "AINotch Island" to quit'

# 2. 删 app
rm -rf "/Applications/AINotch Island.app"

# 3. 清掉本地配置和 hook（可选）
rm -rf ~/.agent-halo
```

第 3 步会同时移除自动给 Claude Code / Codex / Cursor / Amp 装的 hook。如果你只是想暂停 AI 监控但保留 app，直接退出即可。

---

## 6. 常见问题

### Q: 启动后岛没出现？
A: 检查屏幕是否有刘海（M1 以后的 MacBook Pro / Air），或者在 Settings → Notch 里看是否被关掉了。

### Q: AirPods 连了但没显示连接岛？
A: 第一次启动时蓝牙权限有没有给？「系统设置 → 隐私与安全性 → 蓝牙」里勾上 AINotch Island。

### Q: 按 ⌘⇧A 没反应？
A: 「系统设置 → 隐私与安全性 → 辅助功能」里勾上 AINotch Island，让它能监听全局快捷键。

### Q: CPU 占用偶尔会到 20-30%？
A: 高频更新（Buddy 动画 + 多个 AI 同时跑）的瞬时尖刺正常，稳态应在 10% 以下。如果持续高，反馈给项目。

### Q: 装了 hook 后 Claude Code 跑得慢？
A: hook 是轻量级 Swift 二进制，单次 < 5ms 开销。如果有可见卡顿，在 Settings → Agent Halo → Behavior 关掉自动 hook，重启 Claude Code。

### Q: 怎么自定义新 AI 工具的检测？
A: 编辑 `~/.agent-halo/agents.json` 加一条 agent 定义（命令名、显示名、图标颜色），重启 app 生效。具体格式见项目 README。

---

## 7. 项目地址 / 反馈

这是个开源项目，欢迎提 issue / PR。

- 项目仓库：见 GitHub
- License：MIT
- 基于：[DynamicNotch](https://github.com/jackson-storm/DynamicNotch)，参考了 [MioIsland](https://github.com/Chen3167/MioIsland) 和 [SuperIsland](https://github.com/shobhit99/SuperIsland)

---

## 8. 版本

- v1.0.0（2026-05-25）首次开源发布
- 包内 SHA-256：跟 `AINotch Island v1.0.0.dmg.sha256` 文件对照（如随包提供）

如果你看到这个 app 派上用场了——说一声让维护者知道 🦀
